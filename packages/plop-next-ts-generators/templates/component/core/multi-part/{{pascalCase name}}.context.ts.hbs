import { createContext } from "@chakra-ui/react-context";
import { SystemStyleObject } from "@chakra-ui/system";

export const [{{pascalCase name}}StylesProvider, use{{pascalCase name}}Styles] = createContext<
  Record<string, SystemStyleObject>
>({
  name: "{{pascalCase name}}StylesContext",
  hookName: "use{{pascalCase name}}Styles",
  providerName: "<{{pascalCase name}} />",
})
